server:
  port: 8082

swagger:
  ui-path: /swagger-ui.html
  api-docs: /v3/api-docs

spring:
  flyway:
    enabled: true
    table: flyway_schema_history_user
    locations: classpath:db/migration
    baseline-on-migrate: true

app:
  registration-token:
    expiry-minutes: 30
  phone-verification-token:
    expiry-minutes: 30
  email:
    signup-confirmation-subject: "Confirm your Trend account"

management:
  endpoints:
    web:
      exposure:
        include: health, info
